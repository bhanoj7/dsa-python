# Stack and Queue Implementations in Python

## 📘 Overview
This project implements two fundamental data structures — Stack and Queue — using Python’s built-in list.
These are frequently used in algorithms, compilers, operating systems, and backend workflows.

## 🧱 Data Structures Implemented
- **Stack (LIFO)** → Last In, First Out
- **Queue (FIFO)** → First In, First Out

## 🚀 Features
### Stack:
- push() — add element to top
- pop() — remove top element
- peek() — view top element without removing
- size() — number of items
- is_empty() — check if stack is empty

### Queue:
- enqueue() — add element to back
- dequeue() — remove element from front
- peek() — view front element
- size() — number of items
- is_empty() — check if queue is empty


