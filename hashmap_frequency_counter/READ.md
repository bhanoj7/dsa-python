# Hashmap Frequency Counter

A simple script to count frequencies of elements in a list using `collections.Counter`.

## Example

Input: `["apple", "banana", "apple", "orange", "banana", "apple"]`

Output: `{'apple': 3, 'banana': 2, 'orange': 1}`

## Run

```bash
python main.py
